# Hwalingo 프로젝트 가이드

## 프로젝트 소개

Hwalingo는 영어 문장 분석과 단어 복습을 돕는 웹 애플리케이션이다. 문장 분석, 단어장, 퀴즈, 스토리텔링 학습 기능을 제공하며 모든 화면은 반응형 디자인으로 개발한다.

## 기술 및 구조

- 프론트엔드: React, TypeScript, Vite, PWA
- 백엔드: Express, TypeScript, PostgreSQL
- 상태 및 데이터: TanStack Query, Zustand, Dexie
- `frontend/src/pages`: 페이지 컴포넌트
- `frontend/src/components`: 공통 UI
- `backend/src`: API와 데이터베이스
- `docs`: 초기 HTML 화면 시안

주요 경로는 `/study`, `/analysis`, `/vocabulary`, `/quiz`, `/story`, `/profile`, `/login`이다. 데스크톱은 상단, 모바일은 하단 내비게이션을 사용한다.

## 디자인

- 대표 색상: `#008C44`, `#FFD95A`, `#18332A`, `#F7F7F2`
- 아이콘 라이브러리: **Phosphor Icons**
- 밝은 배경, 둥근 카드, 충분한 여백과 절제된 그림자를 사용한다.
- 모든 화면은 데스크톱, 태블릿, 모바일에 대응하는 반응형 디자인으로 개발한다.

### 타이포그래피

정보 밀도가 높은 학습 화면을 고려해 기본 본문은 `15px`을 기준으로 한다. 폼 입력값은 모바일 Safari 자동 확대 방지를 위해 `16px`을 유지하고, 다음 크기 체계를 사용한다.

| 토큰 | 크기 | 주요 용도 |
| --- | ---: | --- |
| `--font-xs` | `11px` | 캡션, 메타데이터, 난이도 배지, 아주 작은 보조 정보 |
| `--font-sm` | `13px` | 입력 라벨, 보조 설명, 옵션, 링크, 카드 세부 내용 |
| `--font-md` | `15px` | 기본 본문, 버튼 문구, 주요 학습 내용 |
| `--font-lg` | `17px` | 카드 및 섹션 제목 |
| `--font-xl` | `22px` | 페이지 및 모달 제목 |
| `--font-2xl` | `28px` | 주요 강조 제목 |

- 본문 줄 높이는 폰트 크기의 `1.45~1.6배`, 제목은 `1.2~1.35배`를 사용한다.
- 모바일의 일반 본문은 중요도에 따라 `14~15px`을 사용한다. 입력값만 모바일 Safari 자동 확대 방지를 위해 `16px` 미만으로 설정하지 않는다.
- 버튼 문구는 `13~15px`, 입력 라벨과 옵션은 `12~13px`, 모바일 내비게이션은 `11px`을 기준으로 사용한다.
- `11px`은 캡션, 배지, 메타데이터 같은 보조 정보에만 사용하고 핵심 학습 내용에는 사용하지 않는다.
- 화면 너비에 따라 제목 크기는 조절할 수 있지만 본문과 입력값의 가독성을 우선한다.

### 공통 Dialog

- 안내, 확인, 상세 설정에는 `frontend/src/components/ui/Dialog.tsx`의 `AlertDialog`, `ConfirmDialog`, `Modal` 공통 컴포넌트를 사용한다.
- `AlertDialog`의 디자인 기준은 `docs/alert.html`이다.
- `ConfirmDialog`의 디자인 기준은 `docs/confirm.html`이다.
- `Modal`의 디자인 기준은 `docs/modal.html`이며 화면 하단에서 올라오는 바텀시트 형태로 사용한다.
- 세 컴포넌트는 공통 타이포그래피 토큰을 사용한다. 제목은 `--font-xl`(`22px`), 설명과 버튼은 `--font-sm`(`13px`)을 적용하되 나머지 시각 디자인은 각 `docs` 시안을 따른다.
- 페이지마다 별도의 Alert, Confirm, Modal 스타일을 만들지 말고 공통 컴포넌트를 재사용한다.

## 개발 원칙

- TypeScript 타입을 명확히 작성한다.
- 기존 컴포넌트와 디자인 패턴을 우선 재사용한다.
- 페이지는 `pages`, 재사용 UI는 `components`에 작성한다.
- API 경로는 `/api` 아래에 작성한다.
- 아이콘은 Phosphor Icons를 사용하고 아이콘 버튼에는 `aria-label`을 제공한다.

## AI 문장 분석 언어 정책

- `/study`의 첫 번째 언어는 사용자가 작성한 문장의 `입력언어`, 두 번째 언어는 사용자가 배우려는 `학습언어`다.
- 입력언어와 학습언어는 같을 수 있다. 언어가 다르면 입력의 의미와 의도를 자연스러운 학습언어 문장으로 옮기고, 같으면 번역하지 않고 학습에 적합한 자연스러운 문장으로 분석한다.
- 분석 카드의 문장, 직독직해 조각, 수준별 패러프레이징, 핵심 어휘와 어휘 예문은 학습언어로 생성한다.
- 직독직해 뜻, 배경 지식, 경고, 핵심 어휘의 기본·문맥 의미, 어원, 기억 팁과 예문 해석은 입력언어와 관계없이 한국어로 생성한다. 배경 지식은 상황, 화자의 의도와 어조, 필요한 문화·용례 맥락을 3문장 이하로 설명한다.
- 직독직해의 문법 역할은 영어 어순에 의존하지 않고 학습언어의 문법에 따라 판정하며, 명시된 주어와 주어 역할을 하는 주제어는 `subject`로 반환해 파란색으로 표시한다.
- `완전한 해석`은 입력·학습언어 조합과 관계없이 항상 한국어 번역과 영어 번역을 함께 제공한다.
- 문장 분석 코드와 AI 요청에서는 번역 방향을 뜻하는 `sourceLanguage`/`targetLanguage`보다 역할이 명확한 `inputLanguage`/`learningLanguage` 명칭을 사용한다.

## 단어장 학습 시스템

### 숙련도와 복습 주기

- 숙련도는 `0~7`의 **8단계**로 UI와 DB에서 동일하게 사용한다. 이를 7단계로 표현하거나 구현하지 않는다.

| 레벨 | 상태 | 정답 후 다음 복습 |
| ---: | --- | --- |
| 0 | 처음 만남 | 즉시 또는 학습 세션 종료 후 |
| 1 | 기억 시작 | 1일 |
| 2 | 단기 기억 | 3일 |
| 3 | 익숙함 | 7일 |
| 4 | 장기 기억 진입 | 14일 |
| 5 | 장기 기억 | 30일 |
| 6 | 마스터 | 60일 |
| 7 | 완전 마스터 | 120일 |

- `mastery_level`은 UI 표시와 기본 복습 주기 결정에 사용한다.
- `mastery_score`는 향후 개인화 알고리즘을 위한 `0~100` 범위의 내부 신뢰도 값으로 유지하고 초기 UI에는 노출하지 않는다.
- 정답 간격은 `[0, 1, 3, 7, 14, 30, 60, 120]`일로 설정한다. 정답 시 숙련도를 최대 7까지 한 단계 올리고, 상승한 단계의 간격으로 `next_review_at`을 계산한다.
- 오답 간격은 레벨 `0~2`에서 10분, `3~4`에서 1일, `5~7`에서 3일로 설정한다. 오답 시 숙련도를 최소 0까지 한 단계 내리되, 다음 복습일은 감소한 레벨의 정답 간격이 아닌 실패 전용 간격으로 계산한다.
- 정답/오답 간격을 비롯한 학습 규칙은 하드코딩하지 말고 `backend/src/config/learningRules.ts`에서 관리한다.
- 숙련도, 복습 일정, XP, 레벨, 랭크, 난이도, 문제 선정 계산은 `backend/src/domain/learning`의 독립적인 순수 함수로 분리하고 시뮬레이션 및 단위 테스트가 가능하게 한다.

### 데이터 모델 원칙

- `vocabularies`는 공용 사전 데이터만 저장한다. 사용자별 숙련도와 복습일을 넣지 않는다.
- 단어 고유 조건은 `(source_language_code, target_language_code, normalized_word, part_of_speech)`를 기준으로 한다. 동음이의어를 확장할 때는 `sense_key`를 추가한다.
- `favorite_vocabularies`는 단어장 저장 여부와 출처만 담당한다. 숙련도와 학습 기록은 넣지 않으며, 저장 해제에는 `removed_at`을 이용한 소프트 삭제를 우선해 재저장 시 이력을 복원할 수 있게 한다.
- 사용자별 학습 상태는 `(user_id, vocabulary_id)`가 고유한 `vocabulary_progress`에서 관리한다. `mastery_level`, `mastery_score`, 정답/오답 횟수와 연속 기록, `last_reviewed_at`, `next_review_at`, `mastered_at`, `algorithm_version`, `algorithm_state`를 포함한다.
- 복습 대상과 숙련도 조회를 위해 `(user_id, next_review_at)`, `(user_id, mastery_level)` 인덱스를 둔다.
- 한 번의 오늘의 테스트는 `quiz_sessions`로 관리하고 `active`, `completed`, `abandoned` 상태 및 문제 수, 정답 수, XP, 시작/완료 시각을 저장해 중단 후 재개할 수 있게 한다.
- `quizzes`는 답안 당시 상태를 보존한다. 세션, 문제 유형, `question_payload`, 변경 전후 숙련도/점수, 다음 복습일, 획득 XP, 예정 복습 여부를 저장한다. `question_payload`에는 당시 문제, 선택지, 정답, 예문을 스냅샷으로 남긴다.
- 사용자 성장 데이터는 `user_progress.total_xp`와 `prestige_level`만 영속화한다. 레벨은 XP 함수로, 랭크는 레벨로 계산하며 파생값을 중복 저장하지 않는다.
- 모든 XP 변경은 `xp_events` 원장에 기록하고 `quiz_id` 고유 조건으로 재요청에 의한 중복 지급을 막는다.
- 학습일, 스트릭, 주간 목표는 사용자 시간대를 반영한 `user_learning_days` 일별 집계로 계산한다. `user_settings`에는 `timezone`(기본 `Asia/Seoul`)과 `weekly_goal_days`(기본 5)를 둔다.
- 위 스키마 변경은 `backend/sql/002_vocabulary_learning.sql` 같은 순차 마이그레이션으로 작성한다.

### 답안 처리와 출제

- 답안 1건의 채점, `quizzes`, `vocabulary_progress`, `xp_events`, 사용자 및 일별 집계 갱신은 하나의 DB 트랜잭션에서 원자적으로 처리한다.
- 초기 문제 유형은 숙련도 `0~2` 객관식, `3~4` 직접 입력, `5~6` 문맥 빈칸, `7` 문장 생성 또는 번역으로 한다.
- 객관식과 직접 입력은 애플리케이션에서 채점한다. 고급 생성형 답안만 AI 평가를 사용하고 구조화된 평가 결과를 받는다. 녹음과 음성 인식이 필요한 말하기 문제는 후속 단계로 둔다.
- 오늘의 테스트는 복습일 경과 단어, 자주 틀린 단어, 새 단어, 오래된 마스터 단어 순으로 우선한다. 20문제 기준 목표 슬롯은 각각 `10/2/5/3`으로 하되 정확한 비율을 강제하지 않는다.
- 동일 단어를 중복 출제하지 않고, 부족한 그룹의 슬롯은 복습 예정 단어에 우선 재배분한다. 연체 기간, 오답률, 마지막 출제일을 정렬에 반영하며 마스터 단어는 오래 출제되지 않은 항목에 가중치를 준다.
- 저장 단어가 적은 초기 사용자는 비율을 적용하지 않고 가능한 단어를 모두 출제한다.

### XP, 레벨, 랭크

- 예정된 복습에서 처음 성공한 경우에만 전체 XP를 지급한다. 힌트 사용 성공은 70%, 너무 이른 임의 테스트는 20~30%, 오답 또는 정답 공개는 0 XP를 지급한다.
- XP는 현재 숙련도만이 아니라 실제 경과 일수와 예정 복습 간격을 기준으로 계산한다.
- 레벨 곡선은 최대 레벨, 초반 배율, 기본 XP, 선형/곡선 증가량, 지수 등의 설정과 계산 함수로 관리한다. 49개의 임계값을 직접 하드코딩하지 않고 시뮬레이션으로 수치를 조정한다.
- 기본 랭크는 레벨 1 `Cadet`, 10 `Scout`, 20 `Ranger`, 30 `Vanguard`, 40 `Elite`, 50 `Hwarang Master`로 설정에서 관리한다. 답안 처리 전후 랭크를 비교해 랭크업을 판단한다.

### 구현 순서와 코드 구조

1. DB 마이그레이션, 단어 upsert, 북마크 저장/해제, 실제 단어장 목록과 분석 화면 연결
2. 테스트 세션, 저장 단어 문제 선정, 객관식/직접 입력, 답안 이력, 숙련도/복습일 갱신, 세션 재개
3. XP 원장, 누적 XP, 레벨/랭크 계산, 중복 지급 방지, 프로필 연결
4. 네 가지 오늘의 테스트 그룹, 슬롯 재분배, 일별 집계, 스트릭과 주간 목표
5. AI 고급 문제, 오답 설명, 생성 결과 캐시와 비용 제한, AI 장애 시 기본 문제 대체
6. 응답 시간/오답 패턴 기반 개인화, 알고리즘 버전 전환, 말하기 평가와 Prestige

- 백엔드는 `config/learningRules.ts`, `domain/learning/{reviewSchedule,mastery,xp,level,rank,questionDifficulty,testSelection}.ts`, `routes/{vocabularies,quizSessions,progress}.ts`를 기준으로 구성한다.
- 프론트엔드는 `services/{vocabulary,quiz,progress}.ts`와 `components/{vocabulary,quiz,progress}`를 역할별로 구성하고 기존 페이지와 연결한다.
- 현재 규모에서는 Repository/Service/Controller 계층을 불필요하게 늘리지 말고 계산 로직의 순수 함수 분리를 우선한다.

## 명령어

```bash
npm run dev
npm run lint
npm run build
```
